module.exports = {

    /**
     * Starts up the app and puts the HTML in the provided container
     * @param divContainer {HTMLElement} The container div
     */
    start: function(divContainer){
        // TODO: Create the models and views
    }

}
